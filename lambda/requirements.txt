# Dependências do Lambda Weather Forecast API
# Para instalar: pip install -r requirements.txt

# HTTP requests
requests==2.32.5

# AWS Lambda Powertools (logging, tracing, event handling)
aws-lambda-powertools==3.23.0

# AWS X-Ray SDK (tracing distribuído)
aws-xray-sdk==2.14.0
